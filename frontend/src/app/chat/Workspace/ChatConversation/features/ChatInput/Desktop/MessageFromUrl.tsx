'use client';

/**
 * @deprecated Deep linking via URL parameters is not supported in the desktop app (Veridium)
 * Kept as empty component to avoid breaking imports if any
 */
const MessageFromUrl = () => {
  return null;
};

export default MessageFromUrl;
