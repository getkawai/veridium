# Foundry
out/
cache/
broadcast/
cache_forge/

# Hardhat (legacy)
node_modules/
artifacts/
typechain/
typechain-types/
deployments.json

# Environment
.env

# Tooling
.DS_Store
coverage/
coverage.json
.docusaurus/

# Note: 'lib' is NOT ignored by default in Foundry if not using submodules. 
# Since we used --no-git, tracking 'lib' ensures dependencies are available on clone.
# If you prefer to download them every time, keep lib/ in gitignore.
